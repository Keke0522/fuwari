---
// src/components/APlayerWrapper.astro
import APlayer from './index.vue'
const props = Astro.props;
---
<APlayer
        client:only="vue"
        src={props.src}
        poster={props.poster}
        title={props.title}
        artist={props.artist}
/>
